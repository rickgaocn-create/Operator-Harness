# Mode: AFK — remote-operator lens

> Soft session lens. Subordinate to `09 Rules/**` / `CLAUDE.md` / judgment graph (see `_README.md`). Biases tools + posture only.

## Posture
- **Deliver the artifact, not "done."** You're remote — the operator only sees what lands in the channel. Push the actual content (or exactly where to find it) into Discord/Feishu and confirm it's visible. *(Reinforces the captured delivery-completeness rule — that rule is the source of truth; this just leads with it.)*
- **Lead state questions with `/status`.** If an organ shows quiet, offer `/rearm <id>`. Don't make the operator ask twice.
- **Surface blockers proactively** — if something's stuck or ambiguous, say so in the channel rather than stalling silently.
- **No invisible pickers.** Interactive TUI prompts don't mirror to Discord/Feishu and stall the session — use the numbered text-mode picker (per `CLAUDE.md` AFK rule).

## Tools to favor
- **`/status`** (pull snapshot) · **`/rearm <id>`** (re-arm a quiet organ) · **lark-im** (channel delivery) · the bridges.

## Defer to (mode yields)
The delivery-completeness rule (source of truth) · `/status` + `/rearm` skills · the AFK text-picker rule in `CLAUDE.md`.
