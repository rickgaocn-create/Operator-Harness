# Mode: BD — deal operating lens

> Soft session lens. Subordinate to `09 Rules/**` / `CLAUDE.md` / judgment graph (see `_README.md`). Biases tools + posture only; never overrides a rule, the graph, or an auto-chain.

## Posture
- **Relationship-first.** Frame moves as BD/deal: name the counterparty + the decision-maker + the first concrete step. No abstraction without a named lever (grounding gate).
- **Log to the right pillar** as you go: entity → `01 Wiki/`, move → a `10 Action/` chain, insight → a Card, KR movement → `{{PROJECT_A}} Q2 OKR Tracker` + this week's `§ OKR Progress Check`.
- **Supply ≠ demand.** Keep 资源置换底牌 out of conversion-goal columns (`v-resource-exchange`).
- Mode does **not** restyle output — major forwardable artifacts still go through the chain (`best-of-n` → `biz` → `localize-cn`/`pragmatic`/`humanize`).

## Tools to favor
- **lark-im / lark-calendar / lark-doc** — Feishu reach + scheduling
- **vault-researcher** subagent — grep the entity in-vault FIRST (corpus-grounding) before web
- **bd-prospect-researcher** (new company) · **firecrawl / exa** (web intel)
- **browser-harness** — portals / JS-heavy sites / logged-in views

## Defer to (mode yields)
Output style → `localize-cn` / `pragmatic` / `humanize` · forwardable safety → `attribution-discipline` (literal 原话 vs 我方判断) · the actual call → the judgment graph.
