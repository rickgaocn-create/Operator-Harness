# 03 Projects — Permanent Project Artifacts

Contracts, briefs, deal memos — persists across cycles. One subfolder per
project, each with its own `CLAUDE.md` hub (directive · people · live-state
pointers — not an override tier). Scaffold a new one with `/new-project`.

> All project content excluded from this public harness.
